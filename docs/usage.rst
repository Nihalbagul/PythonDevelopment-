Usage Guide
===========

To use the text processor tool, provide a file path and specify the operations you want to perform using command-line options.

Example:

.. code-block:: bash

   python text_processor.py -f sample.txt -wc -cc -find "hello"

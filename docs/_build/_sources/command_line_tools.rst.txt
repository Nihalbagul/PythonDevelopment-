Command-Line Tools
==================

The text processing tool provides several command-line options to operate on a text file.

Example usage:

.. code-block:: bash

   python text_processor.py -f sample.txt -wc -cc -find "example" -r "old" "new"
